// public/main.js
// ✅ Ya no reconstruimos tarjetas (porque ya están en el HTML con seleccionar.html?gradoId=...)

document.addEventListener("DOMContentLoaded", () => {
  // Si luego quieres cargar grados desde API, lo hacemos,
  // pero manteniendo el flujo seleccionar.html?gradoId=...
});